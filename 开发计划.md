# 智慧农业硬件系统 - 开发计划

> **版本**: 2.0.0
> **更新日期**: 2026-07-26
> **目标平台**: 树莓派 5（Raspberry Pi OS / Linux ARM64）+ 7 寸电容触摸屏
> **服务器平台**: Windows 11 + Next.js
> **App 平台**: Android Studio

---

## 一、项目概述

### 1.1 项目目标

构建一个生产级、可远程升级的物联网硬件网关，具备：

- **传感器自动发现**：扫描 I2C / GPIO / ADC / 1-Wire 接口，自动注册发现的设备
- **数据上传至云端**：默认上传所有扫描到的设备，支持白名单/黑名单过滤
- **配置热加载**：通过触摸屏修改配置后无需重启即可生效
- **OTA 远程升级**：支持 HTTP 拉取和 Git Pull 双模式，含备份和回滚
- **触摸屏 UI**：基于 Tkinter 的苹果风格简约界面，支持 1024×600 全屏
- **离线缓存重传**：网络异常时本地 SQLite 缓存，恢复后自动重传

### 1.2 核心特性

| 特性 | 描述 |
| --- | --- |
| 模块化架构 | 6 层分离：UI / App / Core / Services / Drivers / Scanner |
| 非阻塞状态机 | 所有耗时操作在子线程，主线程响应迅速 |
| 配置驱动 | 所有运行参数均可通过 `config/settings.yaml` 配置 |
| 自动降级 | Windows 开发时无 GPIO 自动进入测试模式 |
| 优雅停止 | 收到 SIGTERM/SIGINT 后安全停止所有线程 |
| 事件总线 | 模块间松耦合通信，支持订阅/发布 |

---

## 二、系统架构

### 2.1 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                  UI 层 (ui/)                                │
│  仪表盘 / 网络配置 / 设备管理 / 设备扫描 / 在线升级          │
├─────────────────────────────────────────────────────────────┤
│                  App 层 (app/)                              │
│  System 主控 - 协调各模块、生命周期管理、信号处理            │
├─────────────────────────────────────────────────────────────┤
│                  Core 层 (core/)                            │
│  ConfigManager / EventBus / Logger                          │
├─────────────────────────────────────────────────────────────┤
│                  Services 层 (services/)                    │
│  UploadService / CacheService / HeartbeatService            │
├─────────────────────────────────────────────────────────────┤
│                  OTA 层 (ota/)                              │
│  OTAManager / HTTPUpdater / GitUpdater                     │
├─────────────────────────────────────────────────────────────┤
│              Drivers 层 + Scanner 层                        │
│  Sensors: DHT/BMP280/Vibration                              │
│  Actuators: Relay/Laser/RGB-LED                             │
│  Scanners: I2C/GPIO/ADC/1-Wire                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 目录结构

```
基于树莓派yolo的传感器检测与上传/
├── main.py                    # 统一入口（start/scan/status/config/ota/version）
├── VERSION                    # 当前版本号
├── requirements.txt           # Python 依赖
├── README.md                  # 项目说明（待补充）
├── 开发计划.md                # 本文档
│
├── config/                    # 配置文件（支持热加载）
│   ├── settings.yaml          # 主配置（系统/上传/扫描/OTA/UI）
│   ├── sensors.yaml           # 传感器配置
│   └── actuators.yaml         # 执行器配置
│
├── app/                       # 应用层
│   └── system.py              # System 主控类
│
├── core/                      # 核心层
│   ├── config_manager.py      # 配置管理器（含热加载监控）
│   ├── event_bus.py           # 事件总线
│   └── logger.py              # 日志系统
│
├── services/                  # 服务层
│   ├── upload_service.py      # 数据上传服务
│   ├── cache_service.py       # SQLite 缓存服务
│   └── heartbeat_service.py   # 心跳服务
│
├── ota/                       # OTA 升级层
│   ├── base.py                # 升级器基类与状态枚举
│   ├── manager.py             # OTA 管理器
│   ├── http_updater.py        # HTTP 拉取升级
│   └── git_updater.py         # Git Pull 升级
│
├── drivers/                   # 设备驱动层
│   ├── sensors/               # 传感器驱动
│   │   ├── base.py            # 传感器基类
│   │   ├── dht.py             # DHT11/22 温湿度
│   │   ├── bmp280.py          # BMP280 气压温度
│   │   └── vibration.py       # 振动传感器
│   └── actuators/             # 执行器驱动
│       ├── base.py            # 执行器基类
│       ├── relay.py           # 继电器
│       ├── laser.py           # 激光
│       └── rgb_led.py         # RGB-LED
│
├── scanner/                   # 设备扫描层
│   ├── base.py                # 扫描结果数据类
│   ├── device_scanner.py      # 扫描管理器
│   ├── i2c_scanner.py          # I2C 总线扫描
│   ├── gpio_scanner.py        # GPIO 数字传感器扫描
│   ├── adc_scanner.py         # ADC 模拟传感器扫描
│   └── onewire_scanner.py     # 1-Wire 设备扫描
│
├── ui/                        # 触摸屏 UI
│   ├── main_window.py         # 主窗口（底部 Tab 导航）
│   ├── pages.py               # 各页面实现
│   ├── widgets.py             # 自定义控件
│   └── theme.py               # 苹果风格主题
│
├── utils/                     # 工具函数
├── logs/                      # 日志目录（运行时生成）
├── data/                      # 数据目录（缓存数据库）
├── .backup/                   # OTA 备份目录
└── .downloads/                # OTA 下载目录
```

---

## 三、模块详细设计

### 3.1 配置管理 (`core/config_manager.py`)

**职责**：统一加载、读取、修改、热监控配置文件。

**特性**：
- 支持 `.` 分隔的嵌套键访问（如 `upload.server_url`）
- 5 秒间隔自动检测文件 mtime 变化，触发回调
- 修改后自动持久化到对应 YAML 文件
- 配置段映射：`sensors`→sensors.yaml、`actuators`→actuators.yaml、其余→settings.yaml

**关键配置项**：
- `upload.server_url`：上传服务器地址
- `upload.interval`：上传间隔（秒）
- `upload.upload_filter`：上传过滤模式（all/whitelist/blacklist）
- `ota.mode`：OTA 升级模式（http/git）
- `scanner.enabled_interfaces`：启用的扫描接口

### 3.2 系统主控 (`app/system.py`)

**职责**：协调所有模块的生命周期，处理信号。

**启动流程**：
1. 初始化 ConfigManager
2. 初始化 Logger
3. 初始化 CacheService / UploadService / HeartbeatService / OTAManager / DeviceScanner
4. 加载并初始化传感器和执行器
5. 启动配置热加载监控（5 秒间隔）
6. 启动心跳服务
7. 启动 OTA 自动检查
8. 启动数据采集线程（按 `upload.interval` 周期性采集并上传）
9. 启动缓存重传线程（每 60 秒检查本地缓存）
10. 启动 UI（如启用）

**优雅停止**：
- 收到 SIGINT/SIGTERM 设置 `_stop_event`
- 等待所有工作线程结束（5 秒超时）
- 清理所有 GPIO/I2C 资源
- 发布 `SYSTEM_STOP` 事件

### 3.3 上传服务 (`services/upload_service.py`)

**API 端点**：
- `POST /api/device/report`：批量上传传感器/执行器数据
- `GET /api/device/ack?gateway_ip=xxx`：拉取待执行指令
- `POST /api/device/ack`：发送指令执行回执

**数据格式**：
```json
{
  "gateway_ip": "192.168.1.63",
  "farm_id": 1,
  "area": "温室1号区域",
  "nodes": [
    {"node_id": "T-1-001", "type": "temperature", "name": "空气温度", "value": 25.6, "unit": "°C"}
  ]
}
```

**上传过滤**：
- `all`：上传所有设备（推荐，默认）
- `whitelist`：仅上传 `device_ids` 或 `device_types` 列表中的设备
- `blacklist`：排除 `device_ids` 或 `device_types` 列表中的设备

**失败处理**：
- 立即重试 `max_retries` 次，重试间隔按倍数递增
- 全部失败后写入本地 SQLite 缓存
- 缓存重传线程每 60 秒尝试上传

### 3.4 OTA 升级 (`ota/`)

**双模式架构**：
- **HTTP 模式**：从 `manifest_url` 获取版本信息，下载 tar.gz/zip 代码包，校验 checksum 后解压安装
- **Git 模式**：通过 `git fetch` + `git reset --hard origin/main` 更新代码

**升级流程**：
1. `check_update`：检查是否有新版本
2. `backup_current`：备份当前代码到 `.backup/backup_YYYYMMDD_HHMMSS/`
3. `download`：下载新版本（仅 HTTP 模式）
4. `install`：解压并覆盖项目文件（排除 .git/logs/data 等目录）
5. `verify`：验证关键文件存在且可导入核心模块
6. 失败时自动 `rollback`：从最新备份恢复

**自动升级**：
- `auto_check_enabled`：每小时检查一次
- `auto_update`：发现新版本后自动升级（默认关闭，仅通知）

### 3.5 设备扫描 (`scanner/`)

**支持的接口**：
| 接口 | 检测方法 | 设备类型示例 |
| --- | --- | --- |
| I2C | 扫描 0x03-0x77 地址，查询已知设备表 | BMP280(0x76), MPU6050(0x68), LCD1602(0x27) |
| GPIO | 对每个引脚尝试 `Button(pin)`，读取电平 | 振动/倾斜/按键/PIR |
| ADC | 读取 MCP3008 通道，超过阈值的判定为有设备 | 光照/土壤湿度/声音 |
| 1-Wire | 扫描 `/sys/bus/w1/devices/` 目录 | DS18B20 温度传感器 |

**扫描结果**：
- 转换为 `sensors.yaml` 格式并保存
- 重复扫描不会覆盖已有配置（按 ID 去重）

### 3.6 触摸屏 UI (`ui/`)

**设计原则**：苹果风格简约设计，符合 7 寸触摸屏操作习惯。

**界面结构**：
```
┌────────────────────────────────────┐
│ 状态栏：时间 | 标题 | 退出按钮      │  ← 顶部
├────────────────────────────────────┤
│                                    │
│         当前页面内容                │  ← 中间
│                                    │
├────────────────────────────────────┤
│ 仪表盘 | 网络 | 设备 | 扫描 | 升级  │  ← 底部 Tab
└────────────────────────────────────┘
```

**5 个页面**：
1. **仪表盘**：系统运行状态、设备数量、服务器信息、上传统计
2. **网络配置**：服务器地址、上传间隔、心跳设置（可保存并热加载）
3. **设备管理**：已配置设备列表、上传过滤模式设置
4. **设备扫描**：选择接口、执行扫描、查看结果、保存到配置
5. **在线升级**：检查更新、执行升级、查看进度、回滚

---

## 四、开发流程

### 4.1 已完成工作

- [x] **核心层**：ConfigManager（含热加载）、EventBus、Logger
- [x] **服务层**：UploadService（含过滤）、CacheService、HeartbeatService
- [x] **OTA 层**：OTAManager、HTTPUpdater、GitUpdater（含备份/回滚）
- [x] **扫描层**：DeviceScanner、I2C/GPIO/ADC/OneWire Scanner
- [x] **驱动层**：DHT/BMP280/Vibration 传感器 + Relay/Laser/RGB-LED 执行器
- [x] **UI 层**：MainWindow + 5 个页面 + 自定义控件 + 苹果风格主题
- [x] **应用层**：System 主控（整合所有模块）
- [x] **入口层**：main.py 统一入口（6 个子命令）
- [x] **配置文件**：settings.yaml 完整 8 个配置段
- [x] **依赖清单**：requirements.txt 详细分类

### 4.2 待完成工作

- [ ] **运行测试**：在树莓派上实际部署并测试硬件功能
- [ ] **服务器适配**：确认 Next.js 服务器 API 完全匹配
- [ ] **Android App**：开发移动端监控 App
- [ ] **YOLO 检测**：集成摄像头 YOLO 视觉识别模块（可选扩展）
- [ ] **日志查看**：UI 中添加日志查看器
- [ ] **权限配置**：systemd service 配置开机自启
- [ ] **性能优化**：长时间运行稳定性测试

### 4.3 部署流程

#### 4.3.1 树莓派部署

```bash
# 1. 克隆代码到树莓派
git clone <repo-url> ~/smart-farm
cd ~/smart-farm

# 2. 安装系统依赖
sudo apt-get update
sudo apt-get install -y python3-pip python3-tk python3-dev \
    libgpiod2 i2c-tools spi-tools

# 3. 启用 I2C/SPI/1-Wire 接口
sudo raspi-config nonint do_i2c 0
sudo raspi-config nonint do_spi 0
sudo raspi-config nonint do_onewire 0

# 4. 安装 Python 依赖
pip3 install -r requirements.txt

# 5. 修改配置
nano config/settings.yaml

# 6. 测试运行
python3 main.py status
python3 main.py scan --save
python3 main.py start

# 7. 配置开机自启（systemd）
sudo tee /etc/systemd/system/smart-farm.service <<EOF
[Unit]
Description=Smart Farm IoT System
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/smart-farm
ExecStart=/usr/bin/python3 /home/pi/smart-farm/main.py start --no-ui
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl enable smart-farm
sudo systemctl start smart-farm
```

#### 4.3.2 OTA 升级配置

**HTTP 模式**（推荐生产环境）：
1. 服务器实现 `/api/ota/manifest` 接口，返回 JSON：
   ```json
   {
     "version": "2.0.1",
     "download_url": "https://server/files/smart-farm-2.0.1.tar.gz",
     "checksum": "sha256:abc123...",
     "release_notes": "修复上传异常问题"
   }
   ```
2. 配置 `config/settings.yaml`：
   ```yaml
   ota:
     mode: "http"
     http:
       manifest_url: "https://server/api/ota/manifest"
   ```

**Git 模式**（适合开发环境）：
1. 项目代码托管到 Git 仓库
2. 配置 `config/settings.yaml`：
   ```yaml
   ota:
     mode: "git"
     git:
       enabled: true
       remote: "origin"
       branch: "main"
   ```

### 4.4 开发规范

1. **代码风格**：遵循 PEP 8，使用 `black` 格式化
2. **中文注释**：所有公共方法和类必须中文注释说明用途
3. **错误处理**：所有外部调用必须有 try-except，避免线程崩溃
4. **资源释放**：GPIO/I2C 设备在 `cleanup()` 中释放
5. **配置驱动**：所有可变参数通过 YAML 配置，不硬编码
6. **版本兼容**：使用 `>=` 指定依赖版本，定期更新测试

---

## 五、版本历史

### v2.0.0 (2026-07-26)

**新增功能**：
- 完整模块化架构（6 层分离）
- 配置热加载（5 秒间隔自动检测）
- OTA 升级双模式（HTTP + Git）
- 设备自动扫描（I2C/GPIO/ADC/1-Wire）
- 触摸屏 UI（苹果风格，5 个页面）
- 数据上传过滤（all/whitelist/blacklist）
- 离线缓存重传（SQLite）
- 统一 CLI 入口（main.py）

**移除内容**：
- 删除过时的 `hardware/` 目录
- 删除所有 markdown 文档（重新制定规范）
- 删除过时的入口脚本
- 合并重复的驱动实现

---

## 六、技术支持

- **硬件平台**：树莓派 5 + 7 寸触摸屏
- **操作系统**：Raspberry Pi OS (Linux ARM64)
- **Python 版本**：3.11+
- **配置文件**：YAML（UTF-8 编码）
- **依赖管理**：pip + requirements.txt

如需新增传感器/执行器，遵循以下步骤：
1. 在 `drivers/sensors/` 或 `drivers/actuators/` 创建驱动文件
2. 继承 `BaseSensor` 或 `BaseActuator`
3. 实现 `initialize()` / `read()` 或 `execute_command()` / `cleanup()`
4. 在 `app/system.py` 的 `_get_sensor_driver_map` 注册
5. 在 `config/sensors.yaml` 或 `config/actuators.yaml` 添加配置项
