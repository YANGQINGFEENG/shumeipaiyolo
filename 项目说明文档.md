# 智慧农业物联网监控平台项目说明文档

## 更新日期：2026-07-26

---

## 1. 项目概述

智慧农业物联网监控平台是一个基于Next.js的现代化Web应用，用于实时监控和管理农业环境传感器数据与执行器设备。该平台提供了直观的仪表盘界面，展示温度、湿度、光照、土壤湿度等关键农业环境参数，支持设备自动发现、IP区域划分、多类型执行器控制、数据趋势分析和AI智能决策等功能。

### 主要功能

- **实时数据监控**：传感器数据实时采集与展示
- **设备自动分类**：硬件上报数据后自动识别传感器/执行器类别
- **IP区域划分**：基于IP地址自动划分设备区域，同一IP归为同一区域
- **多类型执行器控制**：支持布尔值、整数、角度等五种控制类型
- **控制卡片自适应**：根据硬件上报的control_type自动加载对应控制UI
- **回执确认机制**：发送指令后等待硬件回执，收到回执才更新页面
- **超时提醒**：30秒未收到回执自动标记超时并提醒用户
- **WebSocket实时通信**：指令实时推送，状态实时更新
- **数据趋势分析**：历史数据图表展示与对比分析
- **报警管理**：自定义报警规则与通知
- **AI智能决策**：基于LLM的智能诊断与自动控制
- **知识库管理**：农业知识库RAG检索增强
- **多农场/多区域管理**：支持多地点多项目管理
- **响应式设计**：适配不同设备屏幕

---

## 2. 技术栈

### 前端技术

- **框架**: Next.js 16.2.0 (App Router)
- **语言**: TypeScript
- **UI组件**: shadcn/ui
- **样式**: Tailwind CSS v4.2.0
- **图表库**: Recharts
- **图标库**: Lucide React
- **状态管理**: React useState/useEffect
- **实时通信**: WebSocket (原生)

### 后端技术

- **API框架**: Next.js API Routes
- **数据库**: MySQL / SQLite (双支持)
- **实时通信**: WebSocket (ws库)
- **AI服务**: OLLAMA + BAAI/bge-small-zh-v1.5
- **向量存储**: FAISS

### 硬件端

- **主控芯片**: STM32F103C8T6
- **WiFi模块**: ATK-MB026
- **通信协议**: HTTP + WebSocket
- **传感器**: 温度、湿度、光照、土壤湿度等15种
- **执行器**: 水泵、风扇、电机、舵机等11种

---

## 3. 项目结构

```
smart-agriculture/
├── app/                           # 前端页面与API
│   ├── api/                       # API接口
│   │   ├── sensors/               # 传感器相关接口
│   │   ├── actuators/             # 执行器相关接口
│   │   ├── sensor-types/          # 传感器类型接口
│   │   ├── actuator-types/        # 执行器类型接口
│   │   ├── device/                # 设备上报与控制
│   │   │   ├── report/route.ts    # 设备数据上报
│   │   │   └── ack/route.ts       # 控制指令回执
│   │   ├── websocket/route.ts     # WebSocket服务
│   │   ├── alarms/                # 报警相关接口
│   │   ├── strategies/            # 策略相关接口
│   │   ├── farms/                 # 农场相关接口
│   │   ├── ai/                    # AI相关接口
│   │   └── knowledge/             # 知识库相关接口
│   ├── areas/                     # 区域视图页面
│   ├── sensor/                    # 传感器详情页面
│   ├── compare/                   # 数据对比页面
│   ├── detailed/                  # 详细数据页面
│   ├── alarms/                    # 报警管理页面
│   ├── globals.css                # 全局样式
│   ├── layout.tsx                 # 布局组件
│   └── page.tsx                   # 首页/仪表盘
├── components/                    # 组件
│   ├── dashboard/                 # 仪表盘相关组件
│   │   ├── sensor-card.tsx        # 传感器卡片
│   │   ├── actuator-card.tsx      # 执行器控制卡片
│   │   └── ...
│   ├── theme-provider.tsx         # 主题提供者
│   ├── theme-toggle.tsx           # 主题切换组件
│   └── ui/                        # 基础UI组件
├── lib/                           # 工具库
│   ├── db.ts                      # 数据库操作
│   ├── device-sync.ts             # 设备同步服务
│   ├── device-types.ts            # 设备类型字典
│   └── time.ts                    # 时间工具
├── docs/                          # 技术文档
│   ├── api-reference.md           # API参考文档
│   └── hardware-protocol.md       # 硬件协议文档
├── 文档/                          # 中文项目文档
│   ├── 项目说明文档.md             # 本文件
│   ├── 设备数据绑定方案.md         # 设备绑定与区域划分
│   ├── 网页端控制硬件功能说明.md    # 执行器控制说明
│   ├── 测试报告.md                # 系统测试报告
│   ├── 多地点多项目管理方案.md     # 多农场管理方案
│   └── 项目结构分析报告.md         # 项目架构分析
├── scripts/                       # 脚本
│   ├── update-db-v2.ts            # 数据库结构更新脚本
│   └── update-sqlite-db.ts        # SQLite数据库更新脚本
├── public/                        # 静态资源
├── package.json                   # 项目配置
└── tsconfig.json                  # TypeScript配置
```

---

## 4. 数据库设计

### 4.1 核心表结构

#### sensor_types 表（传感器类型）

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 类型ID |
| type | VARCHAR(50) | NOT NULL UNIQUE | 类型标识（temperature/humidity等） |
| name | VARCHAR(100) | NOT NULL | 类型名称 |
| unit | VARCHAR(20) | NOT NULL | 单位 |
| prefix | VARCHAR(10) | DEFAULT 'S' | ID前缀 |
| description | TEXT | - | 描述 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### sensors 表（传感器设备）

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | VARCHAR(50) | PRIMARY KEY | 传感器ID（格式：PREFIX-gatewayId-nodeId） |
| name | VARCHAR(100) | NOT NULL | 传感器名称 |
| type_id | INT | NOT NULL, FOREIGN KEY | 传感器类型ID |
| location | VARCHAR(200) | NOT NULL | 安装位置 |
| area | VARCHAR(100) | DEFAULT '' | 区域名称 |
| farm_id | INT | DEFAULT 0 | 农场ID |
| status | VARCHAR(20) | DEFAULT 'offline' | 在线状态（online/offline） |
| battery | INT | DEFAULT 100 | 电池电量（0-100） |
| signal_strength | INT | DEFAULT NULL | 信号强度（0-100） |
| firmware_version | VARCHAR(50) | DEFAULT NULL | 固件版本 |
| last_update | TIMESTAMP | NULL | 最后更新时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### sensor_data 表（传感器数据）

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 数据ID |
| sensor_id | VARCHAR(50) | NOT NULL, FOREIGN KEY | 传感器ID |
| value | DECIMAL(10, 2) | NOT NULL | 传感器数值 |
| unit | VARCHAR(20) | DEFAULT NULL | 单位（冗余存储） |
| quality | INT | DEFAULT 100 | 数据质量（0-100） |
| timestamp | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 数据采集时间 |

#### actuator_types 表（执行器类型）

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 类型ID |
| type | VARCHAR(50) | NOT NULL UNIQUE | 类型标识（water_pump/motor等） |
| name | VARCHAR(100) | NOT NULL | 类型名称 |
| prefix | VARCHAR(10) | DEFAULT 'A' | ID前缀 |
| control_type | VARCHAR(20) | DEFAULT 'boolean' | 默认控制类型 |
| control_min | DECIMAL(10,2) | DEFAULT 0 | 默认最小值 |
| control_max | DECIMAL(10,2) | DEFAULT 100 | 默认最大值 |
| control_step | DECIMAL(10,2) | DEFAULT 1 | 默认步进值 |
| description | TEXT | - | 描述 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### actuators 表（执行器设备）

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | VARCHAR(50) | PRIMARY KEY | 执行器ID（格式：PREFIX-gatewayId-nodeId） |
| name | VARCHAR(100) | NOT NULL | 执行器名称 |
| type_id | INT | NOT NULL, FOREIGN KEY | 执行器类型ID |
| location | VARCHAR(200) | NOT NULL | 安装位置 |
| area | VARCHAR(100) | DEFAULT '' | 区域名称 |
| farm_id | INT | DEFAULT 0 | 农场ID |
| status | VARCHAR(20) | DEFAULT 'offline' | 在线状态（online/offline） |
| state | VARCHAR(20) | DEFAULT 'off' | 运行状态（on/off） |
| mode | VARCHAR(20) | DEFAULT 'auto' | 控制模式（auto/manual） |
| control_value | DECIMAL(10,2) | DEFAULT NULL | 当前控制值 |
| control_type | VARCHAR(20) | DEFAULT 'boolean' | 控制类型 |
| control_min | DECIMAL(10,2) | DEFAULT 0 | 控制最小值 |
| control_max | DECIMAL(10,2) | DEFAULT 100 | 控制最大值 |
| control_step | DECIMAL(10,2) | DEFAULT 1 | 控制步进值 |
| control_default | DECIMAL(10,2) | DEFAULT 0 | 控制默认值 |
| last_update | TIMESTAMP | NULL | 最后更新时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| locked | TINYINT | DEFAULT 0 | 锁定状态（0=未锁定，1=锁定） |

#### actuator_commands 表（控制指令）

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 指令ID |
| actuator_id | VARCHAR(50) | NOT NULL, FOREIGN KEY | 执行器ID |
| command | VARCHAR(20) | NOT NULL | 控制命令（on/off/value） |
| control_value | DECIMAL(10,2) | DEFAULT NULL | 控制值 |
| status | VARCHAR(20) | DEFAULT 'pending' | 指令状态 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| executed_at | TIMESTAMP | NULL | 执行时间 |

**指令状态说明**:
- `pending`: 待执行
- `executing`: 执行中
- `executed`: 已执行（收到回执）
- `failed`: 执行失败
- `timeout`: 超时

#### gateways 表（设备网关）

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 网关ID |
| farm_id | INT | NOT NULL | 农场ID |
| name | VARCHAR(100) | NOT NULL | 网关名称 |
| gateway_type | VARCHAR(50) | NOT NULL | 网关类型 |
| ip_address | VARCHAR(50) | - | IP地址 |
| mac_address | VARCHAR(50) | - | MAC地址 |
| protocol | VARCHAR(20) | - | 通信协议 |
| status | VARCHAR(20) | DEFAULT 'online' | 状态 |
| area | VARCHAR(100) | DEFAULT '' | 区域名称 |
| last_heartbeat | TIMESTAMP | - | 最后心跳时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### farms 表（农场/基地）

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 农场ID |
| name | VARCHAR(100) | NOT NULL | 农场名称 |
| description | TEXT | - | 描述 |
| location | VARCHAR(200) | - | 地理位置 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

---

## 5. 核心功能说明

### 5.1 设备自动发现与分类

硬件设备通过 `/api/device/report` 接口上报数据后，服务器自动完成：

1. **网关自动创建**：根据IP地址自动创建或关联网关
2. **设备类型识别**：根据 `type` 字段识别设备类别（传感器/执行器）
3. **ID自动生成**：统一生成格式 `{PREFIX}-{gatewayId}-{nodeId}`
4. **自动分类存储**：传感器数据存入sensors表，执行器数据存入actuators表
5. **类型自动注册**：新设备类型自动注册到对应类型表
6. **区域自动划分**：基于IP地址自动划分区域

### 5.2 IP区域划分机制

1. **同一IP同一区域**：同一IP地址上报的所有设备默认属于同一区域
2. **自定义区域名**：硬件上报时可通过 `area` 字段指定区域名称
3. **节点级覆盖**：单个设备可通过 `nodes[].area` 覆盖网关级区域设置
4. **区域视图**：`/areas` 页面按区域分组展示所有设备

### 5.3 执行器多类型控制

支持五种控制类型，硬件通过 `control_type` 字段指定：

| 控制类型 | 说明 | 适用设备 |
|---------|------|---------|
| boolean | 布尔值控制（on/off） | 水泵、LED灯、电磁阀 |
| integer | 整数值控制（0-100） | 电机速度、风扇档位 |
| angle | 角度控制（0-180/360） | 舵机、阀门开度 |
| float | 浮点值控制 | 精确参数调节 |
| string | 字符串指令 | 自定义指令、模式切换 |

### 5.4 回执确认与超时机制

**控制流程**：
1. 前端发送控制请求
2. 服务器创建指令（pending），锁定执行器
3. 通过WebSocket推送指令到硬件端
4. 前端显示"执行中"状态
5. 硬件执行后发送回执（command_ack）
6. 服务器更新指令状态为executed，更新执行器状态，解锁
7. 通过WebSocket通知前端更新UI

**超时机制**：
- 超时时间：30秒
- 超时处理：指令标记为timeout，执行器自动解锁，前端显示超时提醒
- 支持重新发送指令

### 5.5 WebSocket实时通信

**连接地址**：`ws://localhost:8080`

**核心消息类型**：
- `command`: 服务器→硬件端，控制指令推送
- `command_ack`: 硬件端→服务器，命令回执
- `command_status`: 服务器→前端，命令状态更新
- `area_sync`: 前端→服务器，订阅区域数据
- `area_update`: 服务器→前端，区域数据更新广播
- `heartbeat`: 双向，心跳检测

---

## 6. API接口文档

### 6.1 设备上报与控制API

#### POST /api/device/report
- **功能**: 硬件设备上报传感器和执行器数据
- **详细说明**: 参见《设备数据绑定方案.md》

#### POST /api/device/ack
- **功能**: 硬件端确认控制指令执行结果（HTTP回执）
- **详细说明**: 参见《网页端控制硬件功能说明.md》

#### GET /api/device/ack
- **功能**: 硬件端查询待执行指令（HTTP轮询用）

### 6.2 传感器API

| 接口 | 方法 | 功能 |
|------|------|------|
| /api/sensors | GET | 获取传感器列表 |
| /api/sensors/[id] | GET | 获取单个传感器详情 |
| /api/sensors | POST | 新增传感器 |
| /api/sensors/[id] | DELETE | 删除传感器 |
| /api/sensors/[id]/data | GET | 获取传感器历史数据 |
| /api/sensors/[id]/data | POST | 新增传感器数据 |

### 6.3 执行器API

| 接口 | 方法 | 功能 |
|------|------|------|
| /api/actuators | GET | 获取执行器列表 |
| /api/actuators/[id] | GET | 获取单个执行器详情 |
| /api/actuators | POST | 新增执行器 |
| /api/actuators/[id] | DELETE | 删除执行器 |
| /api/actuators/[id]/commands | POST | 发送控制指令 |
| /api/actuators/[id]/commands | GET | 查询待执行指令 |

### 6.4 完整API文档

完整的API参考文档请参见：
- `docs/api-reference.md` - 完整API参考（英文版）
- `docs/hardware-protocol.md` - 硬件通信协议

---

## 7. 部署指南

### 7.1 环境要求

- Node.js 18+
- MySQL 8.0+ 或 SQLite
- npm 或 pnpm
- （可选）OLLAMA 用于AI功能

### 7.2 安装步骤

1. **克隆项目**:
   ```bash
   git clone <项目仓库地址>
   cd smart-agriculture
   ```

2. **安装依赖**:
   ```bash
   npm install
   ```

3. **配置数据库**:

   **MySQL方式**:
   - 启动MySQL服务，创建数据库 `smart_agriculture`
   - 执行数据库初始化脚本

   **SQLite方式**（快速开发）:
   - 无需额外安装，项目自动创建SQLite数据库文件

4. **配置环境变量**:
   创建 `.env` 文件:
   ```
   # 数据库配置
   DATABASE_URL="mysql://root:password@localhost:3306/smart_agriculture"
   # 或使用SQLite
   # DATABASE_URL="file:./dev.db"
   
   # WebSocket端口
   WS_PORT=8080
   
   # AI配置（可选）
   OLLAMA_BASE_URL="http://localhost:11434"
   ```

5. **初始化数据库**:
   ```bash
   # 执行数据库结构更新
   npx tsx scripts/update-db-v2.ts
   ```

6. **启动开发服务器**:
   ```bash
   npm run dev
   ```

7. **访问应用**:
   - 前端页面: http://localhost:3000
   - WebSocket: ws://localhost:8080

### 7.3 生产部署

```bash
# 构建生产版本
npm run build

# 启动生产服务器
npm start
```

---

## 8. 项目维护

### 8.1 文档索引

| 文档 | 位置 | 说明 |
|------|------|------|
| 项目说明文档 | 文档/项目说明文档.md | 本文档 |
| 设备数据绑定方案 | 文档/设备数据绑定方案.md | 设备上报、分类、区域划分 |
| 网页端控制硬件功能说明 | 文档/网页端控制硬件功能说明.md | 执行器控制、回执、超时 |
| 测试报告 | 文档/测试报告.md | 系统功能与性能测试 |
| 多地点多项目管理方案 | 文档/多地点多项目管理方案.md | 多农场管理 |
| API参考文档 | docs/api-reference.md | 完整API接口说明 |
| 硬件协议文档 | docs/hardware-protocol.md | 硬件通信协议 |

### 8.2 常见问题

**Q: API返回404错误？**
- 检查API路由文件是否在正确位置
- DELETE方法必须放在 `[id]/route.ts` 中

**Q: 数据不显示？**
- 检查数据库连接配置
- 确认传感器数据已正确上报
- 检查时间格式是否为北京时间（YYYY-MM-DD HH:MM:SS）

**Q: 执行器控制后状态不更新？**
- 检查硬件端是否发送了回执
- 确认WebSocket连接是否正常
- 查看服务器日志和浏览器控制台

**Q: 控制卡片显示不正确？**
- 检查执行器的control_type字段
- 确认硬件上报时是否包含control_type和control_range
- 刷新页面重新加载

### 8.3 扩展建议

- 添加用户认证与权限管理
- 支持MQTT物联网协议
- 实现设备OTA固件升级
- 添加数据加密传输
- 完善的报警通知（短信、邮件、微信）
- 移动端APP开发
- 历史数据批量导出
- 高级数据分析与预测

---

## 9. 版本历史

### v2.0.0 (2026-07-26)

**重大更新**：
- ✅ 设备自动发现与分类机制
- ✅ IP区域自动划分，同一IP设备归为同一区域
- ✅ 支持五种执行器控制类型（boolean/integer/angle/float/string）
- ✅ 控制卡片根据control_type自动加载
- ✅ 控制指令回执确认机制
- ✅ 30秒超时提醒与自动解锁
- ✅ WebSocket实时通信（指令推送、状态更新）
- ✅ HTTP轮询备份模式
- ✅ 区域视图页面（/areas）
- ✅ 完整的API参考文档
- ✅ MySQL和SQLite双数据库支持

### v1.5.0 (2026-04-13)

- ✅ 移除模拟数据，使用真实数据库
- ✅ 设备状态智能判断（正常/延迟/异常/离线）
- ✅ 自动刷新功能（5-10秒）
- ✅ 支持7种传感器类型
- ✅ 低电量警告功能

### v1.0.0 (初始版本)

- ✅ 基础仪表盘功能
- ✅ 传感器数据展示
- ✅ 历史数据图表
- ✅ 设备管理基础功能

---

## 10. 项目总结

智慧农业物联网监控平台是一个功能完整的农业物联网解决方案，通过现代化的前端技术和后端API，实现了传感器数据的实时采集、展示、分析以及执行器的远程智能控制。

### 项目亮点

1. **设备自动发现**：硬件上报数据后自动创建设备，无需手动配置
2. **智能分类管理**：自动识别传感器/执行器类别，分类存储管理
3. **IP区域划分**：基于IP地址自动划分区域，设备管理更清晰
4. **多类型控制**：支持五种控制类型，满足各种执行器需求
5. **控制卡片自适应**：根据硬件配置自动加载对应控制UI
6. **回执确认机制**：发送指令等待硬件回执，确保控制闭环可靠
7. **实时通信**：WebSocket实时推送，延迟<100ms
8. **双模式通信**：WebSocket + HTTP轮询，确保通信可靠性
9. **现代化UI**：苹果风格简约设计，响应式布局
10. **完整文档**：详细的中文文档，便于开发和维护

### 技术优势

- **现代化技术栈**：Next.js 16 + TypeScript + Tailwind CSS
- **高可靠性**：指令可追踪，状态管理完善，超时自动处理
- **强扩展性**：模块化设计，易于添加新设备类型和功能
- **多数据库支持**：MySQL和SQLite双支持，适配不同场景
- **前后端一体**：Next.js全栈开发，开发效率高

通过该平台，农业生产者可以实时监控环境参数，远程控制执行设备，结合AI智能决策，实现精准农业管理，提高生产效率和作物质量。
