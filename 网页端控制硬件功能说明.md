# 网页端控制硬件执行器功能说明

## 更新日期：2026-07-26

---

## 功能概述

实现了网页端远程控制硬件执行器的完整功能。用户可以在网页上通过各种控制卡片（开关、滑块、角度选择等）控制硬件执行器，控制指令通过服务器实时推送给硬件端，硬件执行后发送回执确认，服务器收到回执后才更新页面状态，确保控制闭环。

### 核心特性

- **多种控制类型**：支持布尔值、整数、角度、浮点、字符串五种控制类型
- **实时通信**：WebSocket实时推送控制指令，减少轮询开销
- **回执确认机制**：发送指令后等待硬件回执，收到回执才更新页面
- **超时提醒**：30秒未收到回执自动标记超时并提醒用户
- **区域自动划分**：基于IP地址自动划分设备区域，同一IP设备归为同一区域
- **控制卡片自适应**：根据硬件上报的控制类型自动加载对应控制UI
- **指令锁定机制**：指令执行期间锁定执行器，防止重复发送

---

## 架构设计

### 数据流程

```
网页端 → 服务器（WebSocket/HTTP） → 硬件端 → 执行设备
  ↓           ↓                        ↓          ↓
发送指令   存储指令（pending）       接收指令    执行指令
  ↓           ↓                        ↓          ↓
等待回执   推送指令到硬件            执行回执    反馈状态
  ↓           ↓                        ↓          ↓
更新状态   更新数据库（executed）   发送回执    完成执行
```

### 技术方案

采用 **WebSocket实时推送 + HTTP轮询备份** 的双模式方案：

1. **WebSocket模式（推荐）**：
   - 硬件端与服务器建立WebSocket长连接
   - 服务器实时推送控制指令到硬件端
   - 硬件端通过WebSocket发送回执确认
   - 实时性高，延迟低（<100ms）

2. **HTTP轮询模式（备份）**：
   - 硬件端定期查询服务器获取待执行指令
   - 硬件端执行指令后通过HTTP上报回执
   - 适用于不支持WebSocket的设备

---

## 执行器控制类型

### 五种控制类型

| 控制类型 | 说明 | 交互控件 | 适用设备示例 |
|---------|------|---------|------------|
| boolean | 布尔值控制（on/off） | 开关按钮 | 水泵、LED灯、电磁阀、继电器 |
| integer | 整数值控制（0-100） | 滑块 + 数值输入 | 电机速度、风扇档位、亮度调节 |
| angle | 角度控制（0-180/360） | 角度滑块 + 刻度 | 舵机、阀门开度、遮阳板角度 |
| float | 浮点值控制 | 精细滑块 + 小数输入 | 精确参数调节 |
| string | 字符串指令 | 下拉选择 / 文本输入 | 自定义指令、模式切换 |

### 控制类型配置

硬件端在上报数据时通过 `control_type` 和 `control_range` 字段指定控制类型：

```json
{
  "node_id": "motor_001",
  "type": "motor",
  "state": "on",
  "control_value": 60,
  "control_type": "integer",
  "control_range": {
    "min": 0,
    "max": 100,
    "step": 5,
    "default": 0
  }
}
```

---

## 数据库设计

### 执行器表（actuators）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(50) | 执行器ID（主键），格式：{PREFIX}-{gatewayId}-{nodeId} |
| name | VARCHAR(100) | 执行器名称 |
| type_id | INT | 执行器类型ID |
| type | VARCHAR(50) | 执行器类型标识 |
| location | VARCHAR(200) | 安装位置 |
| area | VARCHAR(100) | 区域名称 |
| status | VARCHAR(20) | 在线状态：online/offline |
| state | VARCHAR(20) | 运行状态：on/off |
| mode | VARCHAR(20) | 控制模式：auto/manual |
| control_value | DECIMAL(10,2) | 当前控制值（电机速度、舵机角度等） |
| control_type | VARCHAR(20) | 控制类型：boolean/integer/angle/float/string |
| control_min | DECIMAL(10,2) | 控制最小值 |
| control_max | DECIMAL(10,2) | 控制最大值 |
| control_step | DECIMAL(10,2) | 控制步进值 |
| control_default | DECIMAL(10,2) | 控制默认值 |
| farm_id | INT | 农场ID |
| last_update | TIMESTAMP | 最后更新时间 |
| locked | TINYINT | 锁定状态（0=未锁定，1=锁定） |

### 控制指令表（actuator_commands）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 指令ID（主键，自增） |
| actuator_id | VARCHAR(50) | 执行器ID |
| command | VARCHAR(20) | 控制命令：on/off/value |
| control_value | DECIMAL(10,2) | 控制值（command=value时使用） |
| status | VARCHAR(20) | 指令状态：pending/executing/executed/failed/timeout |
| created_at | TIMESTAMP | 创建时间 |
| executed_at | TIMESTAMP | 执行时间 |

---

## API接口

### 1. 发送控制指令（网页端）

**接口地址**: `POST /api/actuators/[id]/commands`

**功能说明**: 发送控制指令到指定执行器。

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| command | string | 是 | 控制命令：on / off / value |
| control_value | number | 否 | 控制值（command=value时必填） |

**请求示例（布尔值控制）**:
```json
{
  "command": "on"
}
```

**请求示例（数值控制）**:
```json
{
  "command": "value",
  "control_value": 75
}
```

**响应示例**:
```json
{
  "success": true,
  "data": {
    "id": 123,
    "actuator_id": "MT-1-1001",
    "command": "value",
    "control_value": 75,
    "status": "pending",
    "created_at": "2026-07-26T10:30:00.000Z"
  },
  "message": "指令已发送，等待硬件执行"
}
```

---

### 2. 查询控制指令（硬件端轮询用）

**接口地址**: `GET /api/actuators/[id]/commands`

**功能说明**: 硬件端查询待执行的控制指令（HTTP轮询模式使用）。

**响应示例**:
```json
{
  "success": true,
  "data": {
    "id": 123,
    "actuator_id": "MT-1-1001",
    "command": "value",
    "control_value": 75,
    "status": "pending",
    "created_at": "2026-07-26T10:30:00.000Z"
  },
  "message": "OK"
}
```

---

### 3. 确认指令执行（硬件端HTTP回执）

**接口地址**: `POST /api/device/ack`

**功能说明**: 硬件端确认控制指令执行结果（HTTP方式回执）。

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| gateway_ip | string | 否 | 网关IP地址 |
| actuator_id | string | 是 | 执行器ID |
| command_id | number | 是 | 命令ID |
| status | string | 是 | 执行状态：executed / failed |
| control_value | number | 否 | 实际控制值 |
| state | string | 否 | 执行器状态：on / off |

**请求示例**:
```json
{
  "gateway_ip": "192.168.1.100",
  "actuator_id": "MT-1-1001",
  "command_id": 123,
  "status": "executed",
  "control_value": 75,
  "state": "on"
}
```

**响应示例**:
```json
{
  "success": true,
  "message": "OK",
  "command_id": 123,
  "actuator_id": "MT-1-1001",
  "status": "executed",
  "timestamp": "2026-07-26T10:30:05.000Z"
}
```

---

### 4. 硬件端获取待执行指令列表

**接口地址**: `GET /api/device/ack`

**功能说明**: 硬件端通过网关IP查询所有待执行的指令（批量查询）。

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| gateway_ip | string | 是 | 网关IP地址 |

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 123,
      "actuator_id": "MT-1-1001",
      "command": "value",
      "control_value": 75
    },
    {
      "id": 124,
      "actuator_id": "LT-1-1002",
      "command": "on",
      "control_value": null
    }
  ],
  "count": 2
}
```

---

## WebSocket实时通信

### WebSocket连接

**连接地址**: `ws://localhost:8080`

### 消息类型

| 类型 | 方向 | 说明 |
|------|------|------|
| heartbeat | 双向 | 心跳检测 |
| heartbeat_ack | 双向 | 心跳响应 |
| welcome | 服务器→客户端 | 连接成功欢迎消息 |
| command | 服务器→硬件端 | 控制指令推送 |
| command_ack | 硬件端→服务器 | 命令回执（硬件端） |
| command_status | 服务器→前端 | 命令状态更新（通知前端） |
| gateway_register | 硬件端→服务器 | 网关注册 |
| area_sync | 前端→服务器 | 订阅区域数据 |
| area_update | 服务器→前端 | 区域数据更新广播 |

### 控制指令推送（服务器→硬件端）

```json
{
  "type": "command",
  "data": {
    "command_id": 123,
    "actuator_id": "MT-1-1001",
    "command": "value",
    "control_value": 75,
    "timestamp": 1722000000000
  }
}
```

### 命令回执（硬件端→服务器）

```json
{
  "type": "command_ack",
  "data": {
    "command_id": 123,
    "actuator_id": "MT-1-1001",
    "status": "executed",
    "control_value": 75,
    "state": "on",
    "timestamp": 1722000005000
  }
}
```

### 命令状态更新（服务器→前端）

```json
{
  "type": "command_status",
  "data": {
    "actuator_id": "MT-1-1001",
    "command_id": 123,
    "status": "executed",
    "control_value": 75,
    "state": "on",
    "timestamp": 1722000005000
  }
}
```

---

## 回执确认与超时机制

### 回执确认流程

```
用户点击控制按钮
      ↓
前端发送控制请求到服务器
      ↓
服务器创建指令（状态：pending）
      ↓
服务器通过WebSocket推送指令到硬件端
      ↓
前端显示"指令发送中..."状态
      ↓
硬件端接收并执行指令
      ↓
硬件端发送回执（command_ack）
      ↓
服务器更新指令状态（executed）
      ↓
服务器更新执行器状态和控制值
      ↓
服务器通过WebSocket通知前端
      ↓
前端更新UI显示最新状态
```

### 超时机制

- **超时时间**：30秒
- **超时触发**：发送指令后30秒未收到回执
- **超时处理**：
  1. 指令状态标记为 `timeout`
  2. 执行器自动解锁（locked=0）
  3. 前端显示超时提醒
  4. 允许用户重新发送指令

### 指令状态说明

| 状态 | 说明 |
|------|------|
| pending | 待执行（已创建，等待推送/执行） |
| executing | 执行中（已推送到硬件，等待回执） |
| executed | 已执行（收到硬件回执确认） |
| failed | 执行失败（硬件返回失败） |
| timeout | 超时（30秒未收到回执） |

---

## 控制卡片自动加载

### 加载逻辑

前端根据执行器的 `control_type` 字段自动加载对应的控制卡片：

1. **优先使用硬件上报的配置**：如果数据库中 `control_type` 有值，使用该配置
2. **使用设备类型字典配置**：如果没有硬件上报的配置，使用设备类型默认配置
3. **默认布尔值控制**：如果都没有配置，默认使用布尔值开关控制

### 控制卡片类型

#### 1. 布尔值控制卡片（boolean）

- **控件**：开关按钮
- **功能**：点击切换 on/off 状态
- **适用**：水泵、LED灯、电磁阀、继电器等

#### 2. 整数控制卡片（integer）

- **控件**：滑块 + 数值输入框
- **范围**：由 `control_min` 和 `control_max` 决定
- **步进**：由 `control_step` 决定
- **适用**：电机速度、风扇档位、亮度调节等

#### 3. 角度控制卡片（angle）

- **控件**：角度滑块 + 角度刻度显示
- **范围**：通常 0-180° 或 0-360°
- **步进**：通常 1°
- **适用**：舵机角度、阀门开度、遮阳板角度等

#### 4. 浮点控制卡片（float）

- **控件**：精细滑块 + 小数输入框
- **精度**：支持小数控制
- **适用**：精确参数调节等

#### 5. 字符串控制卡片（string）

- **控件**：下拉选择框 或 文本输入框
- **适用**：自定义指令、模式切换等

---

## 区域划分机制

### 区域划分规则

1. **IP默认区域**：同一IP地址的设备默认属于同一区域
2. **自定义区域**：硬件上报时可通过 `area` 字段指定区域名称
3. **节点级覆盖**：单个设备可通过 `nodes[].area` 覆盖网关级区域设置

### 区域视图

访问 `/areas` 页面查看区域视图：

- 按区域分组显示所有传感器和执行器
- 实时更新区域内设备状态
- 支持在区域视图直接控制执行器
- 区域内设备状态变化实时推送

---

## 前端使用

### 1. 区域视图（推荐）

访问 `http://localhost:3000/areas`

- 按IP/区域分组显示设备
- 实时更新状态
- 统一的控制界面

### 2. 主控制面板

访问 `http://localhost:3000`

- 在"执行器状态"卡片中找到对应执行器
- 根据控制类型显示对应控制卡片
- 操作后等待回执确认

### 3. 控制操作流程

1. 找到目标执行器的控制卡片
2. 进行控制操作（点击开关、拖动滑块等）
3. 按钮/控件显示"发送中"状态，执行器被锁定
4. 等待硬件执行并返回回执
5. 收到回执后更新UI显示最新状态
6. 如果30秒内未收到回执，显示超时提醒

---

## 硬件端集成

### 方案一：WebSocket方式（推荐）

#### 1. 连接WebSocket服务器

```c
// 连接WebSocket服务器
WebSocket_Connect("ws://服务器IP:8080");
```

#### 2. 注册网关

```json
{
  "type": "gateway_register",
  "data": {
    "gateway_ip": "192.168.1.100",
    "gateway_type": "wifi_gateway",
    "mac": "11:22:33:44:55:66"
  }
}
```

#### 3. 接收控制指令

```c
// 处理WebSocket消息
void WebSocket_OnMessage(const char* message) {
    cJSON* root = cJSON_Parse(message);
    cJSON* type = cJSON_GetObjectItem(root, "type");
    
    if (strcmp(type->valuestring, "command") == 0) {
        cJSON* data = cJSON_GetObjectItem(root, "data");
        int command_id = cJSON_GetObjectItem(data, "command_id")->valueint;
        const char* actuator_id = cJSON_GetObjectItem(data, "actuator_id")->valuestring;
        const char* command = cJSON_GetObjectItem(data, "command")->valuestring;
        
        // 执行控制指令
        ExecuteActuatorCommand(actuator_id, command, control_value);
        
        // 发送回执
        SendCommandAck(command_id, actuator_id, "executed", control_value, state);
    }
    
    cJSON_Delete(root);
}
```

#### 4. 发送回执

```json
{
  "type": "command_ack",
  "data": {
    "command_id": 123,
    "actuator_id": "MT-1-1001",
    "status": "executed",
    "control_value": 75,
    "state": "on"
  }
}
```

---

### 方案二：HTTP轮询方式

#### 1. 定期查询待执行指令

```c
// 每2秒查询一次待执行指令
void CheckPendingCommands() {
    // 发送GET请求查询指令
    // GET /api/device/ack?gateway_ip=192.168.1.100
    
    // 解析响应，获取指令列表
    // 对每条指令执行控制操作
    
    // 执行完成后发送回执
    SendCommandAck_Http(command_id, actuator_id, "executed", value, state);
}
```

#### 2. 发送HTTP回执

```c
// POST /api/device/ack
// Content-Type: application/json
/*
{
  "gateway_ip": "192.168.1.100",
  "actuator_id": "MT-1-1001",
  "command_id": 123,
  "status": "executed",
  "control_value": 75,
  "state": "on"
}
*/
```

---

### 设备数据上报

硬件端不定时上报传感器和执行器数据：

```json
POST /api/device/report
{
  "gateway_ip": "192.168.1.100",
  "gateway_type": "wifi_gateway",
  "mac": "11:22:33:44:55:66",
  "farm_id": 1,
  "area": "温室1号区域",
  "nodes": [
    {
      "node_id": "sensor_001",
      "type": "temperature",
      "value": 25.5,
      "unit": "°C",
      "location": "温室中部"
    },
    {
      "node_id": "motor_001",
      "type": "motor",
      "state": "on",
      "mode": "manual",
      "control_value": 60,
      "control_type": "integer",
      "control_range": {
        "min": 0,
        "max": 100,
        "step": 5,
        "default": 0
      },
      "location": "水泵房"
    }
  ]
}
```

---

## 测试步骤

### 1. 测试环境准备

- 服务器运行在 `http://localhost:3000`
- WebSocket服务运行在 `ws://localhost:8080`
- 数据库已配置并运行

### 2. 模拟硬件上报数据

使用Postman或curl发送设备上报请求：

```bash
curl -X POST http://localhost:3000/api/device/report \
  -H "Content-Type: application/json" \
  -d '{
    "gateway_ip": "192.168.1.100",
    "gateway_type": "wifi_gateway",
    "farm_id": 1,
    "area": "测试区域",
    "nodes": [
      {
        "node_id": "test_motor_001",
        "type": "motor",
        "state": "off",
        "mode": "manual",
        "control_value": 0,
        "control_type": "integer",
        "control_range": {
          "min": 0,
          "max": 100,
          "step": 5,
          "default": 0
        },
        "location": "测试位置"
      }
    ]
  }'
```

### 3. 测试网页端控制

1. 打开浏览器访问 `http://localhost:3000/areas`
2. 找到"测试区域"中的电机执行器
3. 拖动滑块或输入数值发送控制指令
4. 观察状态变化

### 4. 模拟硬件回执

```bash
curl -X POST http://localhost:3000/api/device/ack \
  -H "Content-Type: application/json" \
  -d '{
    "gateway_ip": "192.168.1.100",
    "actuator_id": "MT-1-TESTMOTOR001",
    "command_id": 1,
    "status": "executed",
    "control_value": 75,
    "state": "on"
  }'
```

### 5. 测试超时机制

1. 发送控制指令
2. **不要**发送回执
3. 等待30秒
4. 观察前端是否显示超时提醒

---

## 状态同步机制

### 核心原则

1. **服务器状态优先**：服务器状态是唯一权威状态
2. **硬件状态仅监控**：硬件上传的状态用于服务器监控
3. **强制同步**：当硬件状态与服务器不一致时，硬件必须强制同步
4. **回执确认**：控制指令必须收到回执才更新页面

### 状态同步时机

1. **硬件端定期上报**：每5-10秒上报一次状态
2. **指令执行后**：执行完指令后立即上报新状态
3. **强制同步后**：收到强制同步指令后立即执行并确认
4. **状态变化时**：检测到硬件状态变化时立即上报

---

## 故障排查

### 问题1：网页端发送指令失败

**检查清单**：
- 服务器是否正常运行
- 执行器ID是否正确
- 网络连接是否正常
- 浏览器控制台是否有错误信息

### 问题2：硬件端收不到指令（WebSocket模式）

**检查清单**：
- WebSocket连接是否正常建立
- 网关是否正确注册
- 服务器是否正确关联了网关IP和执行器
- 查看服务器日志是否有错误

### 问题3：硬件端收不到指令（HTTP轮询模式）

**检查清单**：
- 硬件端网络连接是否正常
- 执行器ID是否匹配
- 数据库中是否有pending状态的指令
- 查询参数是否正确（gateway_ip）

### 问题4：指令执行后状态未更新

**检查清单**：
- 硬件端是否正确发送回执
- 回执中的command_id和actuator_id是否正确
- 数据库更新是否成功
- 前端WebSocket连接是否正常
- 查看浏览器控制台和服务器日志

### 问题5：控制卡片显示不正确

**检查清单**：
- 执行器的control_type字段是否正确设置
- 硬件上报时是否包含control_type和control_range
- 设备类型字典中是否有对应配置
- 刷新页面后是否正常

---

## 注意事项

1. **回执必须发送**：硬件端执行完指令后必须发送回执，否则前端会显示超时
2. **指令ID正确**：回执中的command_id必须与收到的指令ID一致
3. **控制值范围**：控制值必须在control_min和control_max范围内
4. **并发控制**：同一时间同一执行器只能有一个待执行指令（locked机制）
5. **网络稳定性**：确保硬件端和服务器之间的网络连接稳定
6. **WebSocket重连**：硬件端应实现WebSocket断开自动重连机制
7. **超时处理**：30秒未收到回执会自动超时，硬件端应尽快处理并回执

---

## 未来优化

1. **指令队列**：支持多个指令排队执行
2. **指令优先级**：支持紧急指令优先执行
3. **批量控制**：支持同时控制多个执行器
4. **执行日志**：完整的指令执行历史记录
5. **离线缓存**：硬件离线时缓存指令，上线后自动下发
