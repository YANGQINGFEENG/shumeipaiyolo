# 硬件数据注入方案

## 更新日期：2026-07-24

## 1. 项目概述

本方案旨在实现STM32硬件设备通过WiFi模块向智能农业服务器注入传感器数据和执行器状态。系统已成功连接到本地WiFi网络，现在需要实现数据的实时上传功能。

## 2. 服务器API接口分析

### 2.1 统一数据上报接口（推荐）

| 接口路径 | 方法 | 功能 | 请求体格式 | 响应格式 |
|---------|------|------|------------|----------|
| `/api/device/report` | POST | 设备数据上报（统一协议） | `{"gateway_id": 1, "farm_id": 1, "nodes": [...]}` | `{"success": true, "message": "...", "processed_nodes": [...]}` |

**统一上报协议优势**：
- 支持传感器和执行器数据统一上报
- 设备自动识别和分类
- 新设备自动注册
- 统一ID生成

### 2.2 传统传感器API（兼容）

| 接口路径 | 方法 | 功能 | 请求体格式 | 响应格式 |
|---------|------|------|------------|----------|
| `/api/sensors` | GET | 获取所有传感器 | N/A | `{"success": true, "data": [...], "total": 10}` |
| `/api/sensors` | POST | 新增传感器 | `{"name": "传感器名称", "type_id": 1, "location": "位置"}` | `{"success": true, "data": {...}}` |
| `/api/sensors/[id]/data` | GET | 获取传感器数据 | N/A | `{"success": true, "data": [...], "stats": {...}, "total": 100}` |
| `/api/sensors/[id]/data` | POST | 新增传感器数据 | `{"value": 25.5}` | `{"success": true, "data": {...}}` |

### 2.3 设备类型字典

#### 传感器类型（14种）

| 类型标识 | 名称 | 单位 | 前缀 |
|----------|------|------|------|
| temperature | 空气温度 | °C | T |
| humidity | 空气湿度 | % | H |
| light | 光照强度 | lux | L |
| soil_moisture | 土壤湿度 | % | SM |
| soil_temperature | 土壤温度 | °C | ST |
| ph | 土壤pH值 | pH | P |
| ec | 土壤电导率 | μS/cm | EC |
| co2 | CO2浓度 | ppm | C |
| pm25 | PM2.5 | μg/m³ | PM |
| water_level | 水位 | cm | WL |
| battery | 电池电量 | % | B |
| pressure | 气压 | hPa | PR |
| vibration | 振动 | mm/s | VB |
| altitude | 海拔 | m | AL |

#### 执行器类型（7种）

| 类型标识 | 名称 | 前缀 |
|----------|------|------|
| water_pump | 水泵 | WP |
| fan | 风扇 | FN |
| heater | 加热器 | HT |
| valve | 电磁阀 | VL |
| light | 补光灯 | LT |
| ventilator | 通风机 | VT |
| fogger | 雾化器 | FG |

## 3. 硬件数据注入方案

### 3.1 网络连接

1. **WiFi连接**：
   - 已成功连接到本地WiFi网络
   - SSID: `8848`
   - 密码: `12345678`

2. **服务器连接**：
   - 服务器地址: 本地服务器IP地址（如 `192.168.1.100`）
   - 端口: `3000`（Next.js默认端口）
   - 协议: HTTP

### 3.2 数据发送策略

1. **发送频率**：
   - 常规数据：每2分钟发送一次
   - 异常数据：当传感器数据超出阈值时立即发送

2. **数据格式**：
   - 使用HTTP POST请求
   - Content-Type: application/json
   - 推荐使用统一上报协议，一次性发送多个设备数据

3. **设备ID映射**（统一格式：`{PREFIX}-{gatewayId}-{nodeId}`）：

| 硬件传感器 | 服务器设备ID | 类型标识 | 类别 |
|-----------|--------------|----------|------|
| DHT11温度 | T-1-001 | temperature | 传感器 |
| DHT11湿度 | H-1-001 | humidity | 传感器 |
| 光照传感器 | L-1-001 | light | 传感器 |
| 土壤湿度 | SM-1-001 | soil_moisture | 传感器 |
| 土壤温度 | ST-1-001 | soil_temperature | 传感器 |
| 土壤电导率 | EC-1-001 | ec | 传感器 |
| 土壤PH值 | PH-1-001 | ph | 传感器 |
| 水泵 | WP-1-001 | water_pump | 执行器 |
| 风扇 | FN-1-001 | fan | 执行器 |

### 3.3 HTTP请求示例

#### 推荐：统一上报协议（同时发送多个设备数据）
```http
POST http://192.168.1.100:3000/api/device/report
Content-Type: application/json

{
  "gateway_id": 1,
  "farm_id": 1,
  "nodes": [
    {
      "node_id": "AA:BB:CC:DD:EE:01",
      "type": "temperature",
      "value": 25.5,
      "unit": "°C"
    },
    {
      "node_id": "AA:BB:CC:DD:EE:02",
      "type": "humidity",
      "value": 60.0,
      "unit": "%"
    },
    {
      "node_id": "AA:BB:CC:DD:EE:03",
      "type": "water_pump",
      "state": "on",
      "mode": "auto"
    }
  ]
}
```

#### 兼容：传统方式（单独发送传感器数据）
```http
POST http://192.168.1.100:3000/api/sensors/T-001/data
Content-Type: application/json

{
  "value": 25.5
}
```

## 4. 硬件实现方案

### 4.1 代码结构

1. **新增文件**：
   - `Hardware/ServerComm.c` - 服务器通信模块
   - `Hardware/ServerComm.h` - 服务器通信头文件

2. **修改文件**：
   - `User/main.c` - 添加数据发送逻辑

### 4.2 核心功能实现

1. **服务器通信模块**：
   - `ServerComm_Init()` - 初始化服务器通信
   - `ServerComm_SendSensorData()` - 发送传感器数据
   - `ServerComm_CheckConnection()` - 检查服务器连接状态

2. **数据发送逻辑**：
   - 读取传感器数据
   - 构建JSON数据
   - 发送HTTP POST请求
   - 处理服务器响应
   - 错误重试机制

### 4.3 错误处理

1. **网络错误**：
   - WiFi连接失败：重新连接
   - 服务器连接失败：重试3次
   - HTTP请求失败：记录错误并重试

2. **数据错误**：
   - 传感器数据异常：标记为无效数据
   - JSON构建失败：使用默认值

## 5. 服务器配置

### 5.1 本地服务器设置

1. **启动服务器**：
   ```bash
   cd e:\tghy\smart-agriculture
   npm run dev
   ```

2. **服务器地址**：
   - 本地地址: `http://localhost:3000`
   - 局域网地址: `http://192.168.1.100:3000`（根据实际IP修改）

### 5.2 设备配置（统一ID格式）

1. **传感器**：
   - T-1-001: 空气温度传感器
   - H-1-001: 空气湿度传感器
   - L-1-001: 光照传感器
   - SM-1-001: 土壤湿度传感器
   - ST-1-001: 土壤温度传感器
   - EC-1-001: 土壤电导率传感器
   - PH-1-001: 土壤PH值传感器

2. **执行器**：
   - WP-1-001: 水泵
   - FN-1-001: 风扇
   - HT-1-001: 加热器
   - VL-1-001: 电磁阀

**设备ID格式**：`{PREFIX}-{gatewayId}-{nodeId}`
- 前缀：设备类型缩写（2-3位大写字母）
- gatewayId：网关ID（数字）
- nodeId：节点ID（截取后4位，大写）

3. **设备自动注册**：
   - 首次上报时，设备会自动注册到系统
   - 无需预先在服务器创建设备记录
   - 设备类型自动识别和分类

## 6. 测试方案

### 6.1 功能测试

1. **连接测试**：
   - 测试WiFi连接
   - 测试服务器连接

2. **数据发送测试**：
   - 发送单传感器数据
   - 发送多传感器数据
   - 测试数据完整性

3. **异常测试**：
   - 网络中断测试
   - 服务器故障测试
   - 传感器异常测试

### 6.2 性能测试

1. **响应时间**：
   - 测量HTTP请求响应时间
   - 优化发送频率

2. **稳定性测试**：
   - 连续运行24小时
   - 记录数据丢失率

## 7. 实施步骤

### 7.1 准备工作

1. **确定服务器IP地址**：
   - 在命令提示符中运行 `ipconfig` 获取本地IP

2. **配置传感器ID**：
   - 确保服务器中已创建对应的传感器

3. **测试服务器API**：
   - 使用Postman或浏览器测试API接口

### 7.2 代码实现

1. **创建服务器通信模块**
2. **修改main.c添加数据发送逻辑**
3. **编译并烧录程序**

### 7.3 测试验证

1. **连接测试**
2. **数据发送测试**
3. **稳定性测试**

## 8. 预期效果

- 硬件设备成功连接到WiFi网络
- 传感器数据实时上传到服务器
- 服务器显示完整的传感器数据
- 系统具备错误处理和自动恢复能力

## 9. 技术要点

- **HTTP通信**：使用AT命令发送HTTP POST请求
- **JSON格式**：正确构建JSON数据格式
- **错误处理**：实现完善的错误处理机制
- **数据同步**：确保数据的实时性和完整性

## 10. 注意事项

- 确保服务器在硬件设备启动前已运行
- 确保网络环境稳定
- 定期检查传感器状态
- 监控服务器存储空间

---

**文档版本**：1.0
**创建日期**：2026-04-10
**更新日期**：2026-04-10