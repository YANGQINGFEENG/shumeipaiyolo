# 设备数据绑定方案

## 更新日期：2026-07-24

## 核心概念

### 设备网关（Gateway）
- 有独立IP的设备，可以是：
  - 单个WiFi传感器（自身就是网关）
  - 多传感器聚合器（LoRa/Zigbee网关）
  - 工业网关（RS485转WiFi）

### 设备节点（Node）
- 没有独立IP的传感器或执行器
- 通过网关上报数据
- 有唯一设备ID（MAC或序列号）

### 设备类别（Category）
- **传感器（Sensor）**：采集环境数据的设备（温度、湿度、光照等）
- **执行器（Actuator）**：执行控制指令的设备（水泵、风扇、加热器等）
- **网关（Gateway）**：数据传输和聚合设备

## 数据库设计

```sql
-- 设备网关表
CREATE TABLE gateways (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    farm_id INTEGER NOT NULL,
    zone_id INTEGER,
    name TEXT NOT NULL,
    gateway_type TEXT NOT NULL,  -- wifi_sensor/lorawan_gateway/serial_gateway
    ip_address TEXT,
    mac_address TEXT,
    protocol TEXT,               -- mqtt/http/lorawan
    status TEXT DEFAULT 'online',
    last_heartbeat TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (farm_id) REFERENCES farms(id)
);

-- 设备节点表（挂在网关下）
CREATE TABLE device_nodes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    gateway_id INTEGER NOT NULL,
    node_id TEXT NOT NULL,        -- 设备唯一ID（MAC/序列号）
    name TEXT NOT NULL,
    node_type TEXT NOT NULL,      -- sensor/actuator
    sensor_type TEXT,             -- temperature/humidity/light等
    location TEXT,
    config TEXT,                  -- JSON配置
    status TEXT DEFAULT 'online',
    last_update TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (gateway_id) REFERENCES gateways(id),
    UNIQUE(gateway_id, node_id)
);

-- 设备数据表（统一存储所有数据）
CREATE TABLE device_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    gateway_id INTEGER NOT NULL,
    node_id TEXT NOT NULL,
    sensor_type TEXT NOT NULL,
    value REAL NOT NULL,
    unit TEXT,
    quality INTEGER DEFAULT 100,  -- 数据质量 0-100
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (gateway_id) REFERENCES gateways(id)
);
```

## 数据上报流程（统一协议）

### 场景1：WiFi直连传感器
```json
POST /api/device/report
{
    "gateway_id": 1,
    "farm_id": 1,
    "nodes": [
        {
            "node_id": "AA:BB:CC:DD:EE:FF",
            "type": "temperature",
            "name": "空气温度传感器1",
            "value": 25.5,
            "unit": "°C",
            "location": "1号温室",
            "firmware_version": "v1.2.0",
            "signal_strength": 85,
            "battery_level": 92
        }
    ]
}
```

### 场景2：网关聚合上报（混合传感器和执行器）
```json
POST /api/device/report
{
    "gateway_id": 1,
    "farm_id": 1,
    "nodes": [
        {
            "node_id": "sensor_001",
            "type": "temperature",
            "value": 24.5,
            "unit": "°C"
        },
        {
            "node_id": "sensor_002", 
            "type": "humidity",
            "value": 65.0,
            "unit": "%"
        },
        {
            "node_id": "actuator_001",
            "type": "water_pump",
            "state": "on",
            "mode": "auto"
        }
    ]
}
```

### 场景3：执行器状态上报
```json
POST /api/device/report
{
    "gateway_id": 1,
    "farm_id": 1,
    "nodes": [
        {
            "node_id": "WP-001",
            "type": "water_pump",
            "name": "1号水泵",
            "state": "on",
            "mode": "auto",
            "location": "泵房A区"
        }
    ]
}
```

### 请求体字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| gateway_id | number | ✅ | 网关ID（首次上报可为0） |
| farm_id | number | ✅ | 基地ID |
| nodes | array | ✅ | 设备节点数据数组 |

### nodes数组元素字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| node_id | string | ✅ | 设备节点唯一标识（MAC地址或序列号） |
| type | string | ✅ | 设备类型（见设备类型字典） |
| name | string | ❌ | 设备名称 |
| value | number | ❌ | 传感器数值（传感器类型必填） |
| unit | string | ❌ | 单位 |
| state | string | ❌ | 执行器状态：on/off（执行器类型必填） |
| mode | string | ❌ | 控制模式：auto/manual |
| location | string | ❌ | 安装位置 |
| firmware_version | string | ❌ | 固件版本号 |
| signal_strength | number | ❌ | 信号强度（0-100） |
| battery_level | number | ❌ | 电池电量（0-100） |

## 自动绑定逻辑

1. **首次上报**：自动创建网关和设备节点
2. **设备类型识别**：根据`type`字段自动识别设备类别（传感器/执行器）
3. **设备自动分类**：传感器数据同步到`sensors`表，执行器数据同步到`actuators`表
4. **ID自动生成**：统一生成格式 `{PREFIX}-{gatewayId}-{nodeId}`（如 SM-1-AB34）
5. **类型自动创建**：新设备类型自动注册到`sensor_types`或`actuator_types`表
6. **IP/MAC匹配**：后续上报自动关联到已有设备
7. **基地绑定**：通过网关的farm_id关联到基地
8. **位置更新**：支持手动调整设备位置

## 支持的设备类型

### 传感器类型（14种）

| 类型标识 | 名称 | 单位 | 前缀 | 适用传感器 |
|----------|------|------|------|------------|
| temperature | 空气温度 | °C | T | DHT11、BMP280、DS18B20 |
| humidity | 空气湿度 | % | H | DHT11、SHT30、SHT40 |
| light | 光照强度 | lux | L | BH1750、TSL2561 |
| soil_moisture | 土壤湿度 | % | SM | FC-28、YL-69、SHT30 |
| soil_temperature | 土壤温度 | °C | ST | DS18B20、土壤温度传感器 |
| ph | 土壤pH值 | pH | P | pH传感器模块 |
| ec | 土壤电导率 | μS/cm | EC | EC传感器模块 |
| co2 | CO2浓度 | ppm | C | MH-Z19B、SCD30 |
| pm25 | PM2.5 | μg/m³ | PM | PMS5003、PMS7003 |
| water_level | 水位 | cm | WL | 超声波水位传感器 |
| battery | 电池电量 | % | B | 电压检测模块 |
| pressure | 气压 | hPa | PR | BMP280、BME280 |
| vibration | 振动 | mm/s | VB | SW-420振动传感器 |
| altitude | 海拔 | m | AL | BMP280、BME280（计算得出） |

### 执行器类型（7种）

| 类型标识 | 名称 | 前缀 | 适用设备 |
|----------|------|------|----------|
| water_pump | 水泵 | WP | 直流水泵、蠕动泵 |
| fan | 风扇 | FN | DC风扇、AC风扇 |
| heater | 加热器 | HT | 加热棒、加热垫 |
| valve | 电磁阀 | VL | 继电器、电磁阀模块 |
| light | 补光灯 | LT | LED补光灯、激光灯 |
| ventilator | 通风机 | VT | 排风扇、通风机 |
| fogger | 雾化器 | FG | 雾化喷头、超声波雾化器 |

## 优势

1. **灵活扩展**：支持任意数量的传感器和执行器
2. **自动发现**：新设备首次上报自动注册
3. **自动分类**：设备自动识别并分类存储到对应表
4. **数据隔离**：通过farm_id实现多基地数据隔离
5. **兼容性好**：支持各种物联网协议和设备
6. **统一管理**：设备类型、ID规则、上报协议统一管理
